<div class="togglable contentholder">
	<a href="#" class="closed title_link">
		Remote Feature: <span class="bold"><%= rf %></span>&nbsp;&nbsp;&nbsp;
		Coverage: <span class="bold"><%= covered(data["products"].map{|e|e[1]}) %>/<%= data["products"].count %></span>
	</a>&nbsp;&nbsp;&nbsp;
		Regex: <%= form_for(data["rule"]) do |f|%> <%= f.text_field :regex, :class => "regexp" %> <%= f.submit "Update" %> <% end %>
	
	<div class="contentholder"><%= render :partial => 'product', :collection => data["products"] %></div>
</div>
	