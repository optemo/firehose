<%= form_for(@heading) do |f| %>
  <% if @heading.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@heading.errors.count, "error") %> prohibited this result from being saved:</h2>

      <ul>
      <% @heading.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <%= f.hidden_field :product_type_id %>
 <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
 </div>
  <div class="field">
    <%= f.label "show order" %><br />
    <%= f.text_field :show_order %>
 </div>
 <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
