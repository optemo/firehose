# SQLite version 3.x
#   gem install sqlite3-ruby (not necessary on OS X Leopard)
<% if `git branch` =~ /^\* bestbuy/
    db = 'bestbuy'
   elsif `git branch` =~ /^\* colors/
    db = 'dev'
   elsif `git branch` =~ /^\* dynamic/
    db = 'dynamic'
   else
    db = 'development'
end %>

development:
    adapter: mysql2
    database: firehose_development
    username: oana
    password: cleanslate
    host: jaguar

production:
    adapter: mysql2
    database: firehose_production
    username: oana
    password: cleanslate
    host: jaguar

test:
    adapter: mysql2
    database: optemo_test
    username: oana
    password: cleanslate
    host: jaguar
    
profile:
    adapter: mysql2
    database: optemo_dynamic
    username: oana
    password: cleanslate
    host: jaguar

# Warning: The database defined as "test" will be erased and
# re-generated from your development database when you run "rake".
# Do not set this db to the same as development or production.
#test:
#  adapter: sqlite3
#  database: db/test.sqlite3
#  timeout: 5000
#
#production:
#  adapter: sqlite3
#  database: db/production.sqlite3
#  timeout: 5000
